# CodeCrafters Backend

FastAPI backend for hackathon project.

## Quick Start

```bash
uv sync                    # Install deps
uv run python main.py      # Run server (port 8000)
```

## Docs

- Swagger UI: http://localhost:8000/docs
- See `backend_doc.md` for full API reference
